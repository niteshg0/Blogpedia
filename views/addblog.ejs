<!DOCTYPE html>
<html lang="en">
<head>
    <%- include("./partials/head") %>
    <title>Add Blog Post</title>
    <style>
        .container {
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
            background-color: #f4f6f9;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }
        .form-group {
            margin-bottom: 1.5em;
        }
        .form-group label {
            font-weight: bold;
        }
        .form-control {
            width: 100%;
            padding: 10px;
            margin-top: 8px;
            border: 1px solid #ccc;
            border-radius: 4px;
        }
        .btn-primary {
            padding: 10px 20px;
            background-color: #4CAF50;
            color: white;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            font-size: 1em;
            font-weight: bold;
        }
        .btn-primary:hover {
            background-color: #45a049;
        }
    </style>
</head>
<body>
    <%- include("./partials/nav") %>
    <div class="container">
        <h2>Add New Blog Post</h2>
        <form action="/blog/add" method="post" enctype="multipart/form-data">
            <div class="form-group">
                <label for="title">Title</label>
                <input type="text" id="title" name="title" class="form-control" placeholder="Enter blog title" required>
            </div>
            <div class="form-group">
                <label for="body">Content</label>
                <textarea id="body" name="body" class="form-control" rows="6" placeholder="Write your blog content here" required></textarea>
            </div>
            <div class="form-group">
                <label for="coverImageURL">Cover Image</label>
                <input type="file" id="coverImageURL" name="coverImage" class="form-control" placeholder="Enter cover image" required>
            </div>
            <input type="hidden" name="createdBy" value="<%= user.id %>">
            <button type="submit" class="btn-primary">Submit Blog</button>
        </form>
    </div>
    <%- include("./partials/script") %>
</body>
</html>
